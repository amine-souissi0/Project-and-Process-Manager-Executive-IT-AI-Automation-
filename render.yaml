services:
  - type: web
    name: ulink-assist-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
      - key: PORT
        value: 5000

